# Project Blueprint — Temple Construction Showcase Website

> **This file is the source of truth.** It describes what the project is, how it's built, and the status of every feature. Workflow rule: **before** implementing a feature, add/update its ledger entry (plan + intent); **after** implementing, update the entry (what was actually done, deviations, verification). Decisions with lasting consequences get an ADR in `docs/adr/`.

## What this project is

A white-label static website showcasing the work of a traditional temple/pond construction contractor — Ankit's relative, a traditionally trained builder (guru-parampara lineage, not an engineer), based in coastal Karnataka. The site presents projects, awards, lineage story, services, and testimonials in a modern, minimal, quietly traditional style. The owners manage content themselves through a simple photo-upload form; Ankit manages everything else.

Full approved design: [`docs/superpowers/specs/2026-07-09-temple-website-design.md`](superpowers/specs/2026-07-09-temple-website-design.md)

## Core requirements (from the owner brief)

1. Showcase work — projects with photos; awards section.
2. Modern, minimal, effective; traditional temple feel; **not flashy or boastful**.
3. Mobile friendly (audience browses on phones).
4. Static and stable — no server, no customer login, nothing to maintain.
5. Admin simple enough for non-technical, semi-literate owners: form + photo upload.
6. White-label: easy to change theme, records, and **whose** site it is — one version per relative.
7. Every project field beyond title + one photo is optional and **plug-n-play**: missing data ⇒ block not rendered, never a broken layout.
8. Languages: English primary, Kannada secondary, Tulu/Tamil optional later.

## Architecture (summary)

```
GitHub repo ──push──▶ Cloudflare Pages build ──▶ live static site
     ▲                                                │
     └────── /admin (Sveltia CMS form) ◀── owner's phone
```

- **Astro** static site; content as markdown/YAML + images, validated by zod schemas (content collections).
- **Sveltia CMS** at `/admin` — GitHub-backed form UI, no server of ours.
- **Cloudflare Pages** free hosting; failed builds keep the previous deploy live.
- **White-label** via per-instance `site.config.ts` (name, contacts, theme preset, enabled languages) — see ADR-0005.
- **Themes** as CSS custom-property presets: `stone` (default), `terracotta`, `ink` — see ADR-0006.

See `docs/adr/` for why each of these was chosen and what was rejected.

## Decision index

| ADR | Decision |
|---|---|
| [0001](adr/0001-static-site-with-git-based-cms.md) | Static site + git-based CMS (over Sheets-backend or dynamic CMS) |
| [0002](adr/0002-astro-as-static-site-generator.md) | Astro as the static site generator |
| [0003](adr/0003-sveltia-cms-for-admin.md) | Sveltia CMS for the owner-facing admin |
| [0004](adr/0004-cloudflare-pages-hosting.md) | Cloudflare Pages for hosting |
| [0005](adr/0005-white-label-via-site-config.md) | White-labeling via one config file per instance |
| [0006](adr/0006-theme-presets-css-custom-properties.md) | Themes as CSS custom-property presets |
| [0007](adr/0007-plug-n-play-optional-content-blocks.md) | Plug-n-play optional content blocks |
| [0008](adr/0008-multilingual-static-routes.md) | Multilingual via static routes with English fallback |

## Feature ledger

Statuses: `planned` → `in progress` → `done` (or `dropped`). Update **before** starting (intent/plan) and **after** finishing (outcome, deviations, how verified).

| # | Feature | Status | Notes |
|---|---|---|---|
| F1 | Project scaffold (Astro, content collections, config, CI-ready build) | done | Astro 5.18 project scaffolded: `package.json` (dev/build/preview/check/test/test:smoke scripts), `astro.config.ts`, `tsconfig.json` (extends `astro/tsconfigs/strict`), `site.config.ts` (placeholder `Shri Builders` identity, typed `SiteConfig`/`Lang`/`ThemeName`), `.nvmrc` (20), placeholder `src/pages/index.astro` rendering `{site.name}` (replaced in F3/Task 6). Task 3 added content collections: `src/content.config.ts` defines `projects`/`awards`/`testimonials`/`about`/`services` via the `glob`/`file` loaders (`astro/loaders`) with zod schemas — a project needs only `title` + ≥1 `photos` entry, every other field optional (ADR-0007 layer 1). Sample fixtures under `src/content/`: `projects/sample-temple.md` (maximal, all fields incl. `beforeAfter`, `facts`, links) and `projects/sample-pond.md` (minimal, title + one photo), plus `awards/sample-award.md`, `testimonials/sample-testimonial.md`, `pages/about.md`, `pages/services.yml`. Placeholder SVG images under `_images/` stand in for real photos per instance. Verified: `npm run build` succeeds and `dist/index.html` contains "Shri Builders"; `npm run check` reports 0 errors/warnings/hints; content schemas confirmed working (see 2026-07-09 Task 3 log entry for the negative-test evidence). |
| F2 | Theme system + `stone` preset (layout, header/footer, typography, motifs) | done | Task 4 shipped the structural shell (theme presets stone/terracotta/ink, `global.css`, `BaseLayout`, `Header`/`Footer`/`StickyContact`/`SectionDivider`/`Seo`). Task 11 completed F2 with the visual polish pass (Warm Stone direction): full-bleed hero with a quiet `--c-bg-alt`→`--c-bg` gradient wash + fluid `clamp()` type scale; `SectionDivider` reworked into flanking gold hairlines around the ❖/▲ glyph (new `--c-divider` token, keyed to `--c-accent` in `ink` where `--motif` is empty so the boundary is a visible saffron line, not an invisible gap) and placed between every home section; consistent fluid vertical rhythm (`.section` padding via `clamp()`); card hover states (ProjectCard image zoom-in + title→gold, buttons brighten/press, map/video/nav links) with a `prefers-reduced-motion` kill-switch; gallery/facts spacing widened and facts grid collapses to one column ≤26rem; global `:focus-visible` ring (2px `--c-text`, the highest-contrast token in every theme); `body` gets `padding-bottom: var(--sticky-h)` below 48rem so the fixed StickyContact bar never overlaps the footer. **WCAG AA fix:** darkened `--c-text-soft` in all three theme files so it clears 4.5:1 on `--c-bg` — stone #8a7c66→#746856 (3.81→5.09), terracotta #8a6f5f→#7e6557 (4.34→5.04), ink #75705f→#716c5c (4.82→5.11); each also clears 4.5:1 on `--c-bg-alt` (the footer bg). All colors still live only in `themes/*.css` (component/view/layout/global color-literal grep empty). Verified: build 15 pages, check 0/0/0, test 6/6; terracotta+ink builds link correct theme CSS with no layout breakage (restored to stone, config diff empty); minimal fixture (sample-pond) still renders title+photo only, no optional blocks; no undefined/null/NaN in built HTML. Sticky bar also contributes to F8 (done). |
| F3 | Home page (hero, featured projects, about teaser, awards strip, contact block) | done | Task 6: `src/views/HomeView.astro` renders hero (site.name/tagline/CTA), a single `SectionDivider`, then five independently-guarded sections — featured projects (`featured: true`, falls back to newest-3-by-year when none flagged, section omitted entirely at 0 projects), about teaser (first ~160 chars derived from the about entry's rendered HTML with tags stripped, bilingual via `pickText` against a synthetic `{body_en, body_kn}` object, omitted when empty), awards strip (`.label` + up to 3 `chip`s, newest first, omitted at 0 awards), testimonials (all entries via new `TestimonialCard.astro`, omitted at 0), and an always-shown contact block (call/WhatsApp buttons + region, since `site.phone`/`whatsapp`/`region` are required config fields). `src/components/ProjectCard.astro` added verbatim per brief; `src/components/TestimonialCard.astro` added new (name/quote via `pickText`/role·place meta, all guarded). `src/pages/index.astro` and `src/pages/[lang]/index.astro` are now thin `BaseLayout` + `HomeView` wrappers (placeholder `<h1>` removed from both, resolving the prior inconsistency between the two). Added one new i18n key, `cta_read_more` (en/kn), for the about-teaser link. Verified: `npm run build` clean, `dist/index.html`/`dist/kn/index.html` inspected (hero, featured sample project, about excerpt, awards chip, testimonial, contact block all present; Kannada page shows translated nav/labels with English about-excerpt fallback since `about.md` has no `body_kn`); `grep -riE 'undefined\|\bnull\b\|NaN'` on both pages → empty. Guarding proved negatively: renamed `sample-award.md` away (and cleared `node_modules/.astro` + `.astro` content caches, which otherwise silently serve stale collection data), rebuilt → awards section fully absent (no heading, no empty wrapper, no layout gap), restored the file (byte-identical, confirmed via diff) and rebuilt again → awards section back. Also spot-checked the featured-fallback path: temporarily set `sample-temple.md`'s `featured: false` → both sample projects appeared in "Our work" (fallback to all/newest-3), reverted. `npm run check` → 0/0/0; `npm test` → 6/6. |
| F4 | Projects listing + type filter | done | Task 7: `src/views/ProjectsView.astro` — grid of all projects (newest year first, undefined years last, NaN-safe `?? 0` sort like HomeView) via `ProjectCard`, each wrapped in a `data-type={d.type ?? 'other'}` div. Progressive-enhancement filter bar (ADR-0007): rendered with the `hidden` attribute so no-JS shows no filter UI and every project; an inline `<script>` (verbatim from brief) removes `hidden` and wires click-to-filter. Guarded to render only when ≥2 *explicit* project `type` values are present in the content (an untyped project is always visible under "All" but claims no specific filter button) — with the current 2 sample projects (`temple` + untyped) this guard is `false`, so the bar is absent on the real site today; verified both branches by temporarily giving `sample-pond.md` a second type. `src/pages/projects/index.astro` / `src/pages/[lang]/projects/index.astro` are thin `BaseLayout` + `ProjectsView` wrappers, same pattern as Task 6. No new i18n keys needed — `projects_all`/`projects_filter`/`type_*`/`nav_projects` already existed. Verified: `npm run build` → 4 pages; `dist/projects/index.html` lists both sample projects, filter bar absent (guard correctly triggered); `dist/kn/projects/index.html` exists with Kannada heading/labels; `grep -riE 'undefined\|\bnull\b\|NaN'` empty on all 4 pages; `npm run check` 0/0/0; `npm test` 6/6. |
| F5 | Project detail page (all optional blocks: gallery, description, materials, facts, map, video, before/after) | done | Task 8: `src/views/ProjectDetailView.astro` (verbatim from brief) assembles headline/meta lines (built by joining only the parts that exist), `PhotoGallery` (mandatory, ≥1 photo), then six independently-guarded blocks — description (`pickText`), materials, facts (`hasFacts` = any fact field truthy), before/after, video, map — each block's heading is inline with its guard so a missing field renders neither wrapper nor label. Six new leaf components: `PhotoGallery.astro` (first photo large via `<Image widths={[800,1200]}>`, rest in a thumb grid, `<dialog>` lightbox wired entirely by an inline script — no anchors/buttons around images in the static markup, so JS-off shows plain photos with zero dead UI); `MaterialsChips.astro` (`.chip` list); `FactsGrid.astro` (four independently-guarded fact rows, `duration`/`teamSize`/`ledBy`/`status`); `MapLink.astro` (plain `rel="noopener" target="_blank"` link, no embed); `VideoEmbed.astro` (plain external link always rendered server-side; an inline script parses the href as a URL and only swaps the link for a lazy `youtube-nocookie.com` iframe on click if the host/path genuinely match `youtube.com/watch?v=` or `youtu.be/<id>` — any parse failure or non-YouTube host, e.g. Vimeo, leaves the click's default behavior, i.e. the plain working link, untouched); `BeforeAfter.astro` (two `<figure>`s in a 2-col grid, captioned, no slider JS). Routes `src/pages/projects/[slug].astro` and `src/pages/[lang]/projects/[slug].astro` (verbatim/pattern from brief — the latter cross-products `site.languages.filter(l => l !== 'en')` × all projects in `getStaticPaths`). No new i18n keys — all `detail_*` keys already existed from Task 2. Verified: `npm run build` → 8 pages; `dist/projects/sample-temple/index.html` (maximal fixture) contains all block labels (materials, facts incl. Duration/Team/Led by/Status, before/after captions, video link, map link, built-for); `dist/projects/sample-pond/index.html` (minimal fixture, title + 1 photo only) contains **none** of those labels/blocks — verified by grepping for the absence of every `detail_*` rendered string — and renders only `<h1>` + one `<img>` + the mandatory (non-optional) gallery lightbox scaffolding + the always-present "more projects" link/divider; `grep -riE 'undefined|\bnull\b|NaN'` empty on both fixtures in both languages. `dist/kn/projects/sample-pond/index.html` and `dist/kn/projects/sample-temple/index.html` exist with Kannada block labels. Per-field guarding proved negatively: temporarily dropped `teamSize` from `sample-temple.md`'s `facts` and swapped `videoLink` to a non-YouTube (Vimeo) URL, rebuilt → "Team" row absent while Duration/Led by/Status remained, and the video block still rendered as a plain working link to the Vimeo URL (server-side render is unconditional on the field existing, not on its host); reverted (`diff` byte-identical). Independently simulated the client-side `youtubeId()` parser in Node against 8 cases (www/non-www `youtube.com/watch?v=`, www/non-www `youtu.be/<id>`, Vimeo, a malformed non-URL string, `youtube.com/embed/...`, `m.youtube.com`) — only the two literal forms named in the brief (`youtube.com/watch?v=`, `youtu.be/`) extract an ID; every other case returns `null`, meaning the enhancement never attaches and the link degrades safely. `npm run check` → 0/0/0; `npm test` → 6/6. `grep -n '#[0-9a-fA-F]{3,8}'` across all six new components + the view → no matches (var-only colors). |
| F6 | Awards page | done | Task 9: `src/views/AwardsView.astro` + `src/components/AwardCard.astro`. Awards sorted newest-first (same NaN-safe `?? 0` pattern as Home/Projects). Card shows a photo when present, else a `--c-gold`-bordered 🏆 medallion placeholder (guarded, never both); title always; `givenBy` line only when present (`${t('awards_given_by')}: ${givenBy}`, same join idiom as `ProjectDetailView`'s `builtFor` line); `year` guarded; `note` via `pickText(lang, d, 'note')` guarded. Page itself always renders (it's in the nav); with zero awards only the `<h1>` shows — no grid wrapper, no fake content. |
| F7 | About page (lineage story) + testimonials section | done | Task 9: `src/views/AboutView.astro`. English body renders the `about.md` markdown via `render(entry)` (`astro:content`'s `render`, aliased from `renderEntry` — confirmed this is the correct Astro 5 content-layer API, distinct from Task 6's `rendered.html` string-slicing trick used only for the home-page excerpt); for `kn`, shows the plain-text `body_kn` frontmatter field when present, else falls back to rendering the same English `<Content />` (plug-n-play). `heroPhoto` and `yearsExperience` independently guarded; the whole about-entry block is guarded on the about collection actually having an entry (defensive — the fixture always has one). One new i18n key added to both tables: `about_years_experience` ("years of experience" / "ವರ್ಷಗಳ ಅನುಭವ"), joined as `{yearsExperience} {t(lang,'about_years_experience')}`. Services section reads the `services` collection, guarded on ≥1 entry; each card's name via `pickText(lang, svc.data, 'name')`, blurb via `pickText(lang, svc.data, 'blurb')` independently guarded (the sample `services.yml` has no blurbs, so cards render name-only — verified in the built HTML). Testimonials section at the bottom is verbatim-identical in approach to `HomeView`'s (guarded on ≥1 entry, `TestimonialCard` grid). |
| F8 | Contact page + sticky mobile call/WhatsApp bar | done | Task 4 shipped the sticky mobile call/WhatsApp bar. Task 9 adds the dedicated `/contact/` page: `src/views/ContactView.astro` — big `tel:`/`wa.me` buttons (always shown, `site.phone`/`site.whatsapp` are required config fields), an `email` link guarded on `site.email` (absent in the sample config, confirmed absent from the built page), region line (`t(lang,'contact_region')`: `site.region`), and the owners' names (`site.owners.join(', ')`, guarded on a non-empty array). No contact form — WhatsApp is the contact channel (YAGNI, per brief). |
| F9 | Multilingual (EN/KN routes, strings files, fallback) | done | Task 2 done: i18n core module shipped — `src/lib/i18n.ts` with EN/KN strings, `t()` (English fallback), `pickText()` (empty/whitespace fields fall back to English), `localePath()`, `otherLangs()`; 6 unit tests passing (`tests/i18n.test.ts`). Task 5 added the routing skeleton: `src/lib/routes.ts` (`nonDefaultLangs()`, filters `site.languages` down to non-`en` entries for `getStaticPaths`) and `src/pages/[lang]/index.astro` (same home view as root `index.astro`, but `lang=Astro.params.lang`) — this root/`[lang]` pair is the pattern every remaining page pair copies. Tasks 6–9 added the remaining `[lang]` variants (projects listing, project detail, awards, about, contact); Task 9 shipped the last three (`src/pages/[lang]/{awards,about,contact}/index.astro`), completing the set. Every route now has an English (default, no prefix) and Kannada (`/kn/...`) variant; adding a third language is just adding it to `site.languages` and its string table to `i18n.ts` — no route changes needed since every page pair already reads `Astro.params.lang` generically via `nonDefaultLangs()`. |
| F10 | Sveltia CMS admin (`/admin`, collections, bilingual labels) | done | ADR-0003. Task 13: `public/admin/index.html` (loads Sveltia CMS 0.170.4 from unpkg CDN, pinned exact version, `<meta name="robots" content="noindex">`) + `public/admin/config.yml` (4 collections — projects/awards/testimonials/pages{about,services}). Every CMS field `name` matches the `content.config.ts` zod schema field name **exactly** (verified field-by-field, all collections — see Task 13 report table); bilingual `English · ಕನ್ನಡ` labels + per-field hints (incl. the Google-Maps copy-link hint); `editor: preview: false`; per-collection `_images` media folders beside the content (ADR-0007 `image()` build validation); `facts`/`beforeAfter` as `collapsed` objects; `backend.repo`/`base_url` left as clearly-marked per-instance placeholders (set at deploy, Task 15). **Services reconciliation:** the CMS edits `services.yml` via a `list` widget that serializes as an `items:`-keyed mapping, not the top-level array Task 3's `file()` loader expected — so `src/content/pages/services.yml` was rewrapped under an `items:` key (item fields unchanged) and the `services` loader now unwraps it (`parser: (text) => (parseYaml(text)).items`, `import { parse as parseYaml } from 'yaml'`, new pinned dep `yaml@2.9.0`). Verified: About page still lists all 4 services (EN names on `/about/`, Kannada names on `/kn/about/`), story body still renders (EN + KN fallback); `config.yml` parses as valid YAML; `dist/admin/{index.html,config.yml}` emitted on build with `noindex`; `npm run build` 15 pages, `npm run check` 0/0/0, `npm test` 11/11. **Interactive local-mode CMS test (real Chrome, File System Access API) deferred to Ankit as a manual pre-deploy step** (documented in the Task 13 report) — not runnable headless. |
| F11 | Image pipeline (compression, responsive sizes, dimensions) | done | Task 14: verified end-to-end with a real raster photo (Tasks 6–9's `<Image>`/`astro:assets` usage in `PhotoGallery`/`ProjectCard`/`ProjectDetailView`/`AwardCard`/`BeforeAfter` had only ever been exercised against placeholder SVGs, which Astro passes through untouched). Added a throwaway project (`src/content/projects/zzz-image-test.md` + a generated 4032×3024, 4.5 MB JPEG at `_images/imgtest.jpg`, sized/shaped like a real phone photo), built, and confirmed Astro's sharp-backed pipeline actually transforms raster input: emitted content-hashed `.webp` variants at every requested breakpoint (`imgtest.*_s1xYw.webp` 800w/3.3 KB, `imgtest.*_Jkqct.webp` 1200w/26 KB on the gallery; `imgtest.*_ZzWpIV.webp` 400w/0.86 KB, `imgtest.*_s1xYw.webp` 800w on the card) plus a full-resolution 4032×3024 webp `src` fallback at 3.27 MB (still 26% smaller than the 4.45 MB original from format re-encoding alone). Built `<img>` tags on both the project-detail (`PhotoGallery`) and projects-listing (`ProjectCard`) pages carry real `srcset` (multiple widths) and explicit `width="4032" height="3024"` (prevents layout shift) — see Task 14 report for exact HTML. Confirms the pipeline needs zero code changes to handle real owner-uploaded photos. **Owner-upload rule:** owners upload phone photos as-is (no resizing/compressing needed on their end) — the Astro build pipeline handles all resizing and compression automatically at deploy time. One incidental finding (not a defect): the build also emits one unreferenced original-format copy of the source photo into `dist/_astro/` (not linked from any page/CSS) — harmless dead weight in the deploy, not served to visitors; see Task 14 report. Throwaway project + image removed after verification; `npm run build` back to 15 pages, `npm run check` 0/0/0, `npm test` 11/11. |
| F12 | 404 page, meta/OG tags | done | Task 10: `src/pages/404.astro` — English-only (Cloudflare serves `dist/404.html` verbatim for any unmatched path, so there's no `[lang]` variant), thin `BaseLayout` wrapper rendering `notfound_title`/`notfound_body`/a `notfound_home` link via existing i18n keys (added in Task 2, unused until now). `src/components/Seo.astro` enhanced: when no explicit `image` prop is passed, derives a default `og:image` from the first `featured: true` project's cover photo (`photos[0]`), falling back to the newest project (by `year`, same NaN-safe `?? 0` sort as HomeView/ProjectsView/AwardsView) when nothing is flagged featured, and emitting **no** `og:image` tag at all when the `projects` collection is empty or the chosen project somehow has no photo (ADR-0007 plug-n-play — ran the zero-projects case for real, see work log). Resolves the image through `getImage()` (`astro:assets`) to get the final built asset path, then `new URL(path, site.url)` to make it an absolute URL (required for social-card crawlers, which don't resolve relative `og:image` values) — same absolute-URL pattern the original explicit-`image`-prop branch already used. |
| F13 | Smoke tests (minimal + maximal fixture projects) + schema tests | done | Task 12: `tests/smoke.test.ts` (5 tests) runs against the **built** `dist/` output (via `npm run test:smoke` = `astro build && vitest run tests/smoke.test.ts`, already present from Task 1) to enforce ADR-0007 end-to-end, not just per-task spot checks. `describe.skipIf(!fixturesPresent)` guards the whole suite on `src/content/projects/sample-pond.md` existing, so a real instance that deleted the sample fixtures skips (not fails) rather than false-failing. Five assertions: (1) minimal fixture (`sample-pond`) renders title + `<img>` and **none** of `Method &amp; materials`/`Details`/`Location`/`Watch video`/`Before`/`Built for`, plus no `undefined`/`null`/`NaN` leakage; (2) maximal fixture (`sample-temple`) renders all seven optional-block strings; (3) the `kn` route for the minimal fixture exists and contains Kannada chrome (`ಯೋಜನೆಗಳು`); (4) all seven core pages + `404.html` exist in `dist/`; (5) `dist/index.html` contains the configured identity `Shri Builders` (no leaked placeholder/other-instance name). Every asserted string was verified against the real emitted HTML before being written into the test (not copied blind from the brief) — all matched exactly as drafted, no label adjustments needed. Schema-level negative testing (the "+ schema tests" half of this row) was already covered by Task 3's manual negative test (bad `year` type → build fails with a named field/file error) and is not duplicated here. Test-has-teeth proof (required, not optional): temporarily broke the materials guard in `src/views/ProjectDetailView.astro` — moved the `<h2>Method &amp; materials</h2>` heading outside its `{d.materials && d.materials.length > 0 && ...}` conditional so it always renders (kept `<MaterialsChips>` itself guarded to avoid an unrelated build crash on `undefined.map`) — rebuilt, and the minimal-fixture test failed exactly as expected (`expected ... not to contain 'Method &amp; materials'`), while the other 4 tests still passed; reverted the component (`git diff` empty afterward), rebuilt, all 5 tests passed again. Housekeeping fix required for `npm run check` to stay clean: added `@types/node@^20` as a new devDependency (matching `.nvmrc`'s Node 20) — `tests/smoke.test.ts`'s `node:fs` import has no ambient types otherwise (`astro check` reported `TS2307: Cannot find module 'node:fs'`); no code other than `package.json`/`package-lock.json` changed for this. Verified on the final tree: `npm run test:smoke` → build (15 pages) + 5/5 smoke tests pass; `npm test` → 11/11 (6 i18n + 5 smoke, both under `tests/**/*.test.ts` per `vitest.config.ts`); `npm run check` → 0 errors/warnings/hints. |
| F14 | Deployment (Cloudflare Pages, first instance) | done | Task 15: `docs/DEPLOYMENT.md` — numbered manual runbook (executed by Ankit at go-live, not by the implementing agent): (1) create GitHub repo from this template + the one sanctioned push (`git remote add origin` + `git push -u origin main`); (2) Cloudflare Pages → connect repo, build command `npm run build`, output `dist`, `NODE_VERSION=20` (matches `.nvmrc`); (3) GitHub OAuth app (Settings → Developer settings) with callback pointing at the auth worker; (4) deploy `sveltia-cms-auth` (github.com/sveltia/sveltia-cms-auth) via `npx wrangler deploy`, secrets via `wrangler secret put GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET`, `ALLOWED_DOMAINS` set to the Pages domain; (5) fill the two `# PLACEHOLDER` fields in `public/admin/config.yml` (`backend.repo`, `backend.base_url`) and commit/push; (6) per-instance checklist — every `site.config.ts` field (`name`/`tagline`/`owners`/`phone`/`whatsapp`/`email?`/`region`/`url`/`logo?`/`theme`/`languages`), delete the four `(Sample)`-titled content files + their images, add real content, custom domain later via Pages → Custom domains; (7) the pre-launch interactive CMS smoke test (deferred from Task 13's headless boundary — sign in at the live `/admin`, add a throwaway project, confirm it appears on the live site after ~1 min, delete it, spot-check Services/About round-trip) folded in as the final gate before handing the phone to the owner. This is documentation only — no deploy was actually performed (repo remains local-only, no remote added, per the task's constraints); the runbook is what Ankit will execute when going live. |
| F15 | Owner onboarding (phone login setup, bookmark, walkthrough) | done | Task 15: `docs/ONBOARDING.md` — owner-facing, bilingual (English + Kannada line-by-line) walkthrough. Part 1 (one-time, Ankit-assisted): GitHub account access, open `/admin` in Chrome, sign in once, "Add to Home Screen". Part 2 (owner does solo, anytime): 8-step picture-book-style flow — open the app → tap Projects → tap "+ New" → fill in the name → add a photo (name + 1 photo is the only requirement, everything else optional) → tap Save → wait ~1 minute → see it live — mirroring the actual Sveltia collection labels (`Projects · ಯೋಜನೆಗಳು` etc.) and the real form order from `public/admin/config.yml`. Closes with an explicit reassurance line in both languages: mistakes are never permanent (every save is a git commit — full history, nothing lost) and to call Ankit if anything looks wrong, plus a quick-reference table. Written for a non-technical, semi-literate reader — plain sentences, no jargon. Non-code deliverable; the actual phone setup happens per instance at go-live alongside F14. |
| F16 | Trilingual **content** (per-record kn/hi translations + English fallback) | done | ADR-0009, spec `docs/superpowers/specs/2026-07-10-trilingual-content-design.md`. `lib/types.ts`: optional `i18n?: Translations<T>` on Project/Award/Testimonial/About/SiteInfo/Service, each `T` derived via `Partial<Pick<Record, …>>` so a translation field's type can't drift from the English one. Existing `data/content.json` needed **no migration** (verified: restored the pre-change file and it renders unchanged). `lib/localize.ts`: `overlay()` merges `i18n[lang]` onto the record per field, treating empty/whitespace as absent (explicitly not `??` — the `pickText` bug from the Astro build), and **strips `i18n` from the returned record** so other languages' text isn't serialized into the RSC payload as `ProjectGrid` props (caught during verification: `/kn/projects/` was shipping the Hindi title and every English description in the flight data; now 0 occurrences). `lib/translatable.ts`: the single source of truth for which fields are translatable + their input kind, shared by the forms and `readTranslations()` in `lib/actions.ts`, so the two can't drift. `components/admin/TranslationPanels.tsx`: two collapsed no-JS `<details>` panels per form, each led by its endonym (ಕನ್ನಡ / हिंदी) in the matching Noto Serif face, a gold left rail when any field is translated, and a fill count (`4 of 8 filled` / `Not started`); clearing an input removes the translation. Wired into all five forms (project, award, testimonial, about, settings). 5 new i18n keys. **Verified:** `npx tsc --noEmit` clean; 13/13 unit tests (`npm test`, `node --test`) covering per-field fallback, empty/whitespace-means-absent, array fallback, no-mutation, sidecar stripping — proven to have teeth by reintroducing the `??` bug (3 failed) and reverting; end-to-end through the real admin via plain multipart form POSTs (the no-JS path) → `/kn/projects/sri-venkataramana-temple/` shows the Kannada title/place/materials with the **English description** (per-field fallback), `/hi/` shows the Hindi title with English elsewhere, `/` unchanged; `npm run generate` → same 30 pages + 404, Kannada content in `out/kn/**`, `out/admin` absent. Auto-translate deferred (free backends exist: Bhashini, MyMemory). |
| F17 | Gallery / Album (standalone misc photos, slidable viewer, rotate) | done | Spec `docs/superpowers/specs/2026-07-11-gallery-design.md`. `Content.gallery: string[]` (normalized to [] in readContent — existing content.json needs no migration; not localized, passed through in localizeContent). Admin: `/admin/gallery` reuses `ProjectPhotos` unchanged (submits the same photos/removePhotos fields); new `updateGallery` action mirrors updateProject's photo logic through the atomic writeContent; added to the admin sidebar. Public: `/gallery` reuses `Lightbox` (grid + slider) with an empty-state; 'Gallery' added to the public nav after 'Our Work' (plain <a>, per the lang-routing fix). Lightbox gained: a temporary per-view **rotate** button (CSS transform, resets to 0 on open/next/prev/close, never persisted); **background scroll lock** while open; and **focus management** (moves focus in on open, traps Tab, restores to the triggering thumbnail on close) — the last two were reviewer-flagged defects, fixed here since the gallery centres on Lightbox. Bumped `--c-scrim` 0.9→0.96 so the viewer fully covers the sticky header. `/gallery/` added to snapshot publicPaths; 5 new i18n keys. **Verified** end-to-end: admin POST uploaded 2 photos → content.gallery updated → public /gallery rendered 2 grid items; empty gallery → empty-state, no crash; /kn/gallery/ & /hi/gallery/ localized; headless-Chromium test of the viewer confirmed scroll-lock, focus-in, rotate 90°→180°→reset-on-next, Escape-closes-and-restores-focus-to-thumbnail; `npm run generate` produced out/{,,kn/,hi/}gallery/ with the photos and lang-prefixed nav; tsc clean, 13/13 tests. Test uploads reverted; data/content.json left as the owner's live data (not committed with the feature). |
## Work log

- **2026-07-09** — Brainstormed and approved design (theme direction "Warm Stone" chosen from 3 mockups; project detail blocks approved incl. optional video + before/after). Spec committed. ADRs and this blueprint created. No code yet.
- **2026-07-09** — Task 1 (F1): Scaffolded Astro 5 project skeleton — `package.json`, `astro.config.ts`, `tsconfig.json`, `site.config.ts`, placeholder `src/pages/index.astro`, `.nvmrc`; added `.astro/` to `.gitignore`. Installed `astro@^5`, `typescript`, `@astrojs/check`, `vitest@^3`. Verified with `npm run build && npm run check` (build succeeds, `dist/index.html` contains "Shri Builders", check reports 0 errors).
- **2026-07-09** — Task 2 (F9 core): TDD'd the i18n strings module. Wrote `vitest.config.ts` and `tests/i18n.test.ts` first, confirmed RED (`Cannot find module '../src/lib/i18n'`), then implemented `src/lib/i18n.ts` (EN + KN string tables, `t()` with English fallback for missing langs/keys, `pickText()` for CMS-entry bilingual fields, `localePath()`, `otherLangs()`). Confirmed GREEN: `npm test` → 6/6 passing (includes a regression test that a blank preferred-language field falls back to English); `npm run check` → 0 errors/warnings/hints.
- **2026-07-09** — Task 3 (F1 content layer): Added `src/content.config.ts` with zod schemas for `projects`, `awards`, `testimonials`, `about`, `services` (Astro 5 `glob`/`file` loaders from `astro/loaders`, matching the current installed astro@5.18.2 API exactly — no version-drift adjustments needed). Added sample content and placeholder SVG images: `projects/sample-temple.md` (maximal fixture, every optional field populated) and `projects/sample-pond.md` (minimal fixture, title + one photo only) prove ADR-0007's "title + 1 photo is the only requirement" contract; also `awards/sample-award.md`, `testimonials/sample-testimonial.md`, `pages/about.md`, `pages/services.yml`. Verified `npm run build && npm run check` pass clean. Performed the manual negative test required by ADR-0007 layer 1: set `year: "twenty21"` in `sample-pond.md` → `npm run build` failed (exit 1) with `[InvalidContentEntryDataError] projects → sample-pond data does not match collection schema. year: Expected type "number", received "string"`, naming both the file and field; reverted the edit → build passed again (exit 0), `npm run check` still 0 errors/warnings/hints, `npm test` still 6/6. F1 stays `done` (content-collections portion now complete); lays the foundation for F13 (smoke tests), not yet started.
- **2026-07-09** — Task 4 (F2 + F8 sticky bar): Built the theme system and base layout shell. `src/styles/themes/{stone,terracotta,ink}.css` — three CSS custom-property presets (colors, fonts, `--motif`) per ADR-0006, `stone` is the default in `site.config.ts`. `src/styles/global.css` — reset, typography, layout primitives (`.wrap`, `.section`, `.label`, `.btn`, `.btn-outline`, `.chip`, `.grid-cards`), all referencing `var(--c-*)`/`var(--font-*)` only, no literal colors. Components: `SectionDivider.astro` (motif divider, collapses to nothing when `--motif` is empty, e.g. `ink`), `Header.astro` (logo/monogram fallback, nav via `t()`+`localePath()`, no-JS `<details>` mobile menu, language toggle via `otherLangs()`), `Footer.astro` (name/region/phone link/`footer_rights`/build-time year — not given verbatim in the brief, designed to match the existing var-only, `t()`-only convention), `StickyContact.astro` (mobile-only fixed call/WhatsApp bar, hidden ≥48rem — this is F8's sticky contact bar), `Seo.astro` (title, guarded meta description, canonical from `Astro.url`, OG tags, `og:image` only when passed — also not given verbatim, designed per the brief's prose spec). `src/layouts/BaseLayout.astro` loads the active theme's CSS file dynamically via `import.meta.glob('../styles/themes/*.css', { query: '?url', import: 'default', eager: true })` keyed by `site.theme`, and wires Seo/Header/main/Footer/StickyContact. `src/pages/index.astro` now uses `<BaseLayout lang="en" title={site.name}>` instead of the bare placeholder markup. Verified: `npm run build && npm run check` pass clean (0 errors/warnings/hints); built `dist/index.html` links `/_astro/stone.*.css` by default; flipped `site.theme` to `'ink'`, rebuilt, confirmed the `<link>` href switched to the ink CSS asset, flipped back to `'stone'` and rebuilt again (href back to stone, matches original committed state); `npm run dev` served `/` with `<link ... href="/src/styles/themes/stone.css">` and `<title>Shri Builders</title>`; `grep -rn '#[0-9a-fA-F]\{3,8\}' src/components src/layouts src/styles/global.css` returns no matches (only the three theme files under `src/styles/themes/` contain hex literals, 4 each), confirming ADR-0006's "no literal brand colors in components" rule. `npm test` still 6/6. F2 stays `in progress`: visual/responsive polish (spacing rhythm, hero treatments, real imagery) is Task 11; this task shipped the structural shell every page will build on. F8 gains its sticky mobile contact bar; the rest of F8 (dedicated `/contact/` page) is still open.
- **2026-07-09** — Task 5 (F9 routing skeleton): Added `src/lib/routes.ts` (`nonDefaultLangs()` — filters `site.languages` to non-`en` entries and maps to `{ params: { lang } }` for `getStaticPaths`) and `src/pages/[lang]/index.astro` (imports `BaseLayout`, `site`/`Lang` from `site.config.ts`, and `nonDefaultLangs`; sets `getStaticPaths = nonDefaultLangs`; renders the same home view as root `index.astro` with `lang=Astro.params.lang as Lang` and `path="/"`). This establishes the root-page/`[lang]`-page pattern (root renders `lang="en"`, `[lang]` renders `lang=Astro.params.lang`) that every page pair in Tasks 6–9 copies. Verified: `npm run build` → `dist/kn/index.html` exists (`lang="kn"`, Kannada nav strings from `t()`, canonical `https://example.pages.dev/kn/`), `dist/index.html` unaffected. Removed `'kn'` from `site.languages` in `site.config.ts`, rebuilt → only 1 page built (`/index.html`), no `dist/kn/` directory produced, confirming `getStaticPaths` returns zero paths when a language is disabled; restored `'kn'` (`git diff site.config.ts` empty afterward), rebuilt → `dist/kn/index.html` back. `npm run check` → 0 errors/warnings/hints; `npm test` → 6/6. F9 stays `in progress`: this is routing infrastructure only (one page, the home view) — Tasks 6–9 add the `[lang]` variants for projects/detail/awards/about/contact using this same pattern.
- **2026-07-09** — Task 6 (F3): Built the real home page. `src/components/ProjectCard.astro` (verbatim from the task brief — cover photo via `<Image>`, title, guarded type/place/year meta line). `src/components/TestimonialCard.astro` (new — quote via `pickText`, guarded name/role/place meta). `src/views/HomeView.astro` (new) assembles hero → `SectionDivider` → featured projects → about teaser → awards strip → testimonials → contact block, every section after the hero individually guarded per ADR-0007 (`{items.length > 0 && <section>…}` or equivalent existence check), so the home page degrades gracefully all the way down to hero + contact block if every optional collection is empty. Two design calls made where the brief was prose-only: (1) the about-teaser excerpt is built from the about entry's `rendered.html` (HTML tags stripped, whitespace collapsed, truncated to 160 chars) rather than the `render()` component factory, since `render()` returns an `AstroComponentFactory` with no synchronous plain-text form suitable for slicing — `entry.rendered.html` is the same underlying markdown-compiled data `render()` wraps; bilingual selection reuses `pickText()` against a synthetic `{body_en, body_kn}` object since English is the file's markdown body itself (not a `body_en` frontmatter field) while `body_kn` is a real optional frontmatter field per `content.config.ts`. (2) added one new i18n key, `cta_read_more` (en "Read more" / kn "ಇನ್ನಷ್ಟು ಓದಿ"), for the about-teaser link — no existing key fit. `src/pages/index.astro` and `src/pages/[lang]/index.astro` are now 6-line `BaseLayout` + `HomeView` wrappers (also passing `description={site.tagline}` to `Seo` via `BaseLayout`, previously omitted); the placeholder `<h1>{site.name}</h1>` markup is gone from both, resolving the F1/F9 note about the two pages' placeholders being inconsistent. Verified: `rm -rf dist && npm run build` succeeds; `dist/index.html` — hero (name+tagline+CTA), the featured sample project card, about excerpt + link, awards chip, testimonial, and the contact block (call/WhatsApp buttons + region) all present; `dist/kn/index.html` — Kannada nav/section headings/buttons, project meta's type label translated (`ದೇವಸ್ಥಾನ`), about excerpt correctly falls back to the English text (`about.md` has no `body_kn`). `grep -riE 'undefined|\bnull\b|NaN' dist/index.html dist/kn/index.html` → no matches on both. Guarding proved negatively (the required test): renamed `src/content/awards/sample-award.md` → `.md.bak`; discovered and cleared two content caches that otherwise serve stale collection data across renames (project-local `.astro/` **and** `node_modules/.astro/`) — a note worth keeping for future content-negative-tests; rebuilt → awards section (heading, `<ul>`, chips) fully absent from `dist/index.html`, no empty wrapper, no layout gap; restored the file (confirmed byte-identical via diff) and rebuilt → awards section back. Also spot-checked the featured-fallback branch: set `sample-temple.md`'s `featured: false` → both sample projects appeared under "Our work" (fallback path engaged since neither is now flagged, and there are only 2 total so "newest 3" shows both), reverted. `npm run check` → 0 errors/warnings/hints; `npm test` → 6/6 (unchanged, no new tests needed for this task — precedent from Tasks 4/5, markup-only work). F3 → **done**. F9 unaffected (still `in progress`; remaining `[lang]` page pairs land in Tasks 7–9).
- **2026-07-09** — Task 7 (F4): Built the projects listing page. `src/views/ProjectsView.astro` (new) renders all projects sorted newest-year-first (`(b.data.year ?? 0) - (a.data.year ?? 0)`, undefined years last, same NaN-safe pattern as HomeView) as a `ProjectCard` grid, each card wrapped in a `<div data-type={d.type ?? 'other'}>`. Progressive-enhancement filter bar per ADR-0007: the `.filters` bar is rendered with the `hidden` attribute so with JS disabled no filter UI appears and every project stays visible; the inline `<script>` (copied verbatim from the task brief, byte-for-byte diffed to confirm) removes `hidden` on load and wires button clicks to toggle `[data-type]` element visibility. The bar itself is guarded to render only when ≥2 *explicit* `type` values are present across the content (computed via `Set` over `allProjects.map(p => p.data.type)`, filtering out `undefined` — an untyped project is always visible under "All" but doesn't get its own filter button, since a project having no declared type isn't the same claim as it being explicitly "Other"). With the current sample content (`sample-temple.md` has `type: temple`; `sample-pond.md` has no `type` field at all) there is only 1 explicit type, so the guard evaluates `false` and the filter bar is correctly **absent** from the shipped `dist/projects/index.html` — this was anticipated by the brief itself. Verified the guard's other branch by temporarily adding `type: pond` to `sample-pond.md`, rebuilding, and confirming the bar appeared with `hidden` present in the markup and exactly three buttons (`All`, `Temple`, `Pond / Kalyani`) in the source; reverted the content file (`diff` confirmed byte-identical) before the final build. No new i18n keys needed: `nav_projects`, `projects_all`, `projects_filter`, and all `type_*` keys already existed from Task 2. `src/pages/projects/index.astro` and `src/pages/[lang]/projects/index.astro` (`getStaticPaths = nonDefaultLangs`) are thin `BaseLayout` + `ProjectsView` wrappers, exactly the pattern established in Tasks 5–6, one directory level deeper (import paths adjusted accordingly). Verified on the final committed tree (fresh `node_modules/.astro`/`.astro`/`dist` clear): `npm run build` → 4 pages (`dist/index.html`, `dist/kn/index.html`, `dist/projects/index.html`, `dist/kn/projects/index.html`); `dist/projects/index.html` lists both sample projects (`Shri Durgaparameshwari Temple (Sample)`, `Temple Pond (Sample)`) with `data-type="temple"` / `data-type="other"` wrappers and filter bar absent; `dist/kn/projects/index.html` exists with Kannada `<h1>ಯೋಜನೆಗಳು</h1>` and translated card titles preserved (titles aren't translated, by design — no `title_kn` field in the schema); `grep -riE 'undefined|\bnull\b|NaN'` empty across all 4 pages; `npm run check` → 0 errors/warnings/hints; `npm test` → 6/6 (unchanged). F4 → **done**. F9 unaffected (still `in progress`; remaining `[lang]` page pairs — detail/awards/about/contact — land in Tasks 8–9).
- **2026-07-09** — Task 8 (F5): Built the project detail page and every plug-n-play optional block (ADR-0007's centerpiece). `src/views/ProjectDetailView.astro` (verbatim from brief) — headline/meta lines built by joining only the parts present, mandatory `PhotoGallery`, then six independently-guarded blocks (description via `pickText`, materials, facts via `hasFacts`, before/after, video, map), each guard covering both the block's wrapper and its heading. Six new leaf components (`src/components/{PhotoGallery,MaterialsChips,FactsGrid,MapLink,VideoEmbed,BeforeAfter}.astro`) built to the brief's prose specs (no verbatim code given for these, unlike the view/routes): `PhotoGallery` renders a `<dialog>` lightbox wired up entirely by an inline script (no anchors/buttons in the static markup around images, so JS-off is plain photos, zero dead UI); `VideoEmbed` always server-renders a plain working link and only swaps to a lazy `youtube-nocookie.com` iframe on click if an inline script's URL parser confirms the host/path genuinely match `youtube.com/watch?v=` or `youtu.be/<id>` (any other host, e.g. Vimeo, or a parse failure leaves the plain link untouched — verified both by content mutation and by an independent 8-case Node simulation of the parser); `MapLink` is a plain `rel="noopener" target="_blank"` link with no embed at all (nothing to fail); `FactsGrid` guards each of its four fields (`duration`/`teamSize`/`ledBy`/`status`) independently; `BeforeAfter` is two static `<figure>`s, no slider JS. Routes `src/pages/projects/[slug].astro` (verbatim) and `src/pages/[lang]/projects/[slug].astro` (brief's prose: cross-products `site.languages.filter(l => l !== 'en')` × all projects in `getStaticPaths`, `lang={Astro.params.lang}`). No new i18n keys — all `detail_*` strings already existed from Task 2. Verified on the final committed tree (fresh caches): `npm run build` → 8 pages; `dist/projects/sample-temple/index.html` (maximal fixture, every optional field set) contains every block's rendered label (materials, Duration/Team/Led by/Status, Before/After captions, Watch video, Open in Google Maps, Built for, About this work); `dist/projects/sample-pond/index.html` (minimal fixture, title + 1 photo only) contains **none** of those labels — confirmed by grepping for the absence of every `detail_*` string — and renders only the `<h1>`, one `<img>`, the mandatory (non-optional) gallery/lightbox scaffolding, and the always-present divider + "more projects" link; `grep -riE 'undefined|\bnull\b|NaN'` empty on both fixtures in both languages (the only hits anywhere were the literal JS keyword `null` inside the maximal fixture's `VideoEmbed`/`PhotoGallery` inline scripts — legitimate source code, not rendered page text — confirmed by inspecting the exact match context). `dist/kn/projects/sample-pond/index.html` and `dist/kn/projects/sample-temple/index.html` exist with Kannada block labels (ವಿಧಾನ ಮತ್ತು ಸಾಮಗ್ರಿಗಳು, ವಿವರಗಳು, ಸ್ಥಳ, ವೀಡಿಯೊ ನೋಡಿ, ಮೊದಲು/ನಂತರ, ಯಾರಿಗಾಗಿ, etc.). Guarding proved negatively on real content: temporarily dropped `teamSize` from `sample-temple.md`'s `facts` and swapped `videoLink` to a non-YouTube (Vimeo) URL, rebuilt → the "Team" fact row was absent while Duration/Led by/Status remained (proving FactsGrid's per-field guard, not just the block-level `hasFacts` guard), and the video block still rendered as a normal working link pointed at the Vimeo URL; reverted (`diff` confirmed byte-identical, `git diff --stat src/content/` empty) before the final build/commit. `npm run check` → 0 errors/warnings/hints; `npm test` → 6/6 (unchanged). `grep -n '#[0-9a-fA-F]{3,8}'` across all six new components and the view → no matches (var-only colors, consistent with every prior task). F5 → **done**. Post-review (same day): fixed one literal color the hex-only self-grep had missed — PhotoGallery's lightbox `::backdrop` used `rgba(0,0,0,.7)`; introduced a theme-level `--c-scrim` custom property in all three theme files (`stone`/`terracotta`/`ink`, each a palette-tuned dark translucent scrim) and switched the backdrop to `var(--c-scrim)`, so the full grep `#[0-9a-fA-F]{3,8}|rgba?\(|hsla?\(` across `src/components`+`src/views`+`src/layouts`+`global.css` is now empty (all color literals live only in theme files). Also broadened `VideoEmbed`'s `youtubeId()` to accept `m.youtube.com`/`www.`/`youtube-nocookie.com` hosts and `/embed/<id>`+`/shorts/<id>` paths (still fail-closed → safe link for any unknown host/path), so mobile-pasted links embed inline. Re-verified: build 8 pages, check 0/0/0, test 6/6, minimal fixture still block-free and undefined-free.
- **2026-07-09** — Task 9 (F6, F7, F8): Built the awards, about (services + testimonials), and contact pages — the last three page pairs, completing F9's routing set. `src/components/AwardCard.astro` (new): photo via `<Image>` when `d.photo` is present, else a `--c-gold`-bordered 🏆 medallion placeholder div (mutually exclusive, never both/neither); title always; `givenBy` line built with the same `${t(...)}: ${value}` join idiom as `ProjectDetailView`'s `builtFor` line, guarded; `year` guarded; `note` via `pickText(lang, d, 'note')` guarded. `src/views/AwardsView.astro` (new): awards sorted newest-first (same NaN-safe `?? 0` sort as Home/Projects), `<h1>` always shown (page is always in the nav), the card grid guarded on ≥1 award — zero awards renders heading-only, no empty grid wrapper. `src/views/AboutView.astro` (new): English body via `render(entry)` (`astro:content`'s real content-layer render function, aliased from `renderEntry` — confirmed by reading `node_modules/astro/templates/content/module.mjs`, since Task 6's log flagged `render()`'s return as unsuitable for *string slicing* but it's exactly right for full-body rendering here); Kannada shows `body_kn` when present, else falls back to the same English `<Content />` (plug-n-play, guarded via `Content && <Content />` — needed because `Content` is typed `AstroComponentFactory | undefined` when the `about` collection is empty, and `astro check` correctly rejected an unguarded `<Content />`). `heroPhoto`/`yearsExperience` independently guarded; the whole about block additionally guarded on the about collection having an entry at all. Services section from the `services` collection, guarded on ≥1 entry; each card's name/blurb via `pickText`, blurb independently guarded (the sample `services.yml` has no blurbs — verified cards render name-only in the built HTML). Testimonials section at the bottom reuses `TestimonialCard` exactly as `HomeView` does. `src/views/ContactView.astro` (new): call/WhatsApp buttons always shown (`site.phone`/`whatsapp` are required config fields); `mailto:` link guarded on `site.email` (absent in the sample config — confirmed absent from `dist/contact/index.html`); region line (`t(lang,'contact_region')`: `site.region`); owners' names (`site.owners.join(', ')`), guarded on a non-empty array; no contact form (WhatsApp is the channel, YAGNI per brief). One new i18n key added to both tables: `about_years_experience` ("years of experience" / "ವರ್ಷಗಳ ಅನುಭವ"). Six new thin route files, exact pattern from Tasks 6–8: `src/pages/{awards,about,contact}/index.astro` (`lang="en"`) and `src/pages/[lang]/{awards,about,contact}/index.astro` (`getStaticPaths = nonDefaultLangs`, `lang = Astro.params.lang`) — page `<title>` uses the relevant `nav_*` key + `site.name`, same as Projects/Home. Verified on a fresh build (cleared `dist`/`.astro`/`node_modules/.astro`): `npm run build` → 14 pages total, all six new routes present (`dist/{awards,about,contact}/index.html`, `dist/kn/{awards,about,contact}/index.html`); `dist/about/index.html` shows the `about.md` markdown body, "40 years of experience", all four `services.yml` entries (name-only, no blurbs), and the sample testimonial; `dist/awards/index.html` shows the sample award with its photo (`<img>` present, `.medallion`/🏆 absent — confirmed by grep, proving the photo/placeholder guard is mutually exclusive) and its `givenBy`/year/lack-of-note correctly; `dist/contact/index.html` shows Call/WhatsApp buttons, region, "Owner Name", and confirmed **no** `mailto:` anywhere in the page (sample `site.email` is unset); `dist/kn/about/index.html` falls back to the English markdown body (no `body_kn` in the fixture) while services/testimonials/`about_years_experience` are in Kannada — same fallback shape as `HomeView`'s about-teaser. `grep -riE 'undefined|\bnull\b|NaN'` on all six new pages → empty. `grep -rnE '#[0-9a-fA-F]{3,8}|rgba?\(|hsla?\(' src/components src/views src/layouts src/styles/global.css` → empty (var-only colors, consistent with every prior task). `npm run check` → 0 errors/warnings/hints (one real type error caught and fixed along the way: an unguarded `<Content />` when `about` could be absent — `astro check` correctly flagged `Content` as possibly `undefined`, fixed with `Content && <Content />`). `npm test` → 6/6 (unchanged, no new tests needed — markup-only work, same precedent as Tasks 4/6/7/8). F6 → **done**, F7 → **done**, F8 → **done** (sticky bar from Task 4 + this task's `/contact/` page). F9 → **done**: this was the last task adding `[lang]` route pairs; every page now has an English and Kannada variant.
- **2026-07-09** — Task 10 (F12): 404 page + SEO/OG polish. `src/pages/404.astro` (new) — English-only per the brief (Cloudflare serves `dist/404.html` directly for unmatched paths; no `[lang]` variant needed), thin `BaseLayout lang="en"` wrapper using the three `notfound_*` i18n keys that had sat unused in `src/lib/i18n.ts` since Task 2 (heading, body copy, home link via `localePath('en', '/')`). `src/components/Seo.astro` enhanced with a `defaultOgImage()` helper: when the caller passes no explicit `image` prop, it calls `getCollection('projects')`, picks the first `featured: true` entry (falling back to the newest-by-`year` entry, same `?? 0` NaN-safe sort used everywhere else) and takes its `photos[0]`; if there are zero projects or no cover photo, returns `undefined` and no `og:image` tag is emitted at all — `image` prop still wins when a caller passes one (no existing caller does today, all go through the default). The cover photo (a content-collections `image()`-schema object) is resolved via `getImage()` from `astro:assets` to get its final built asset path, then wrapped in `new URL(path, site.url)` for an absolute URL — same absolute-URL idiom the original `image`-prop branch already used, since og:image needs a real absolute URL for social-card crawlers. Verified on a fully clean build (`dist`/`.astro`/`node_modules/.astro` cleared): `npm run build` → 15 pages incl. `dist/404.html` (`<h1>Page not found</h1>`, body copy, `<a href="/">Go to home page</a>`, full site chrome — header/footer/sticky-contact — since it's a normal `BaseLayout` page); `grep -l 'og:title' dist/**/*.html dist/*.html` → all 16 built HTML files; every page checked has `<title>`, `rel="canonical"`, and `og:site_name`/`og:title`/`og:type`/`og:url`; `dist/index.html`'s `og:image` is `https://example.pages.dev/_astro/temple-1.BNJdyU_j_NdF1.svg` — absolute, `site.url` origin, and correctly sample-temple's cover (the only `featured: true` project). Negative test (required by ADR-0007): renamed both `src/content/projects/sample-{temple,pond}.md` away, cleared caches, rebuilt → `npm run build` exits 0 (Astro logs its standard "collection does not exist or is empty" notice for `projects`, same non-fatal warning every other view's `getCollection('projects')` call already triggers in this state — not a new failure mode), and `grep -c og:image` on every built page (including `dist/404.html`) → `0`; restored both files (`diff` against pre-edit copies confirmed byte-identical) and rebuilt → 15 pages back, `og:image` back. Also spot-checked the "no explicit featured project" branch by temporarily flipping `sample-temple.featured` to `false` and adding `featured: true` to `sample-pond.md`: rebuilt → home page's `og:image` switched to `pond-1.*.svg`, proving the featured-selection branch (not just the zero-projects guard) actually engages; reverted both files (byte-identical diff confirmed) and rebuilt a final time. `npm run check` → 0 errors/warnings/hints; `npm test` → 6/6 (unchanged, no new tests needed — markup/meta-only work, same precedent as Tasks 4/6/7/8/9). `grep -rnE '#[0-9a-fA-F]{3,8}|rgba?\(|hsla?\(' src/components src/views src/layouts src/styles/global.css` → empty. One cosmetic note carried into the report, not fixed: `dist/404.html`'s self-referential `<link rel="canonical" href="https://example.pages.dev/404/">` has a trailing slash (`Astro.url` in `Seo.astro` always reflects the page's own build path, same for every page) even though the actual served path for a missing route has no trailing slash — low-impact (canonical/OG tags on a 404 page are rarely consumed by crawlers) and out of scope for this task's brief, which only asked for the three `notfound_*` strings + a home link. F12 → **done**.
- **2026-07-09** — Task 11 (F2 completion): Visual polish pass across all three themes + shared styles/components — styling only, no content/schema/routing/copy changes (Warm Stone direction per ADR-0006). **Themes (`src/styles/themes/*.css`):** darkened `--c-text-soft` to pass WCAG AA (≥4.5:1 body text) on `--c-bg` in every theme — stone `#8a7c66`→`#746856` (3.81→**5.09:1**), terracotta `#8a6f5f`→`#7e6557` (4.34→**5.04:1**), ink `#75705f`→`#716c5c` (4.82→**5.11:1**); each also verified ≥4.5:1 on `--c-bg-alt` (footer/card background: 4.63/4.62/4.63:1). Added a `--c-divider` token per theme (gold in stone/terracotta; `--c-accent` saffron in ink, since ink repurposes `--c-gold` as near-black and its `--motif` is empty — so the divider stays a visible warm line rather than a dark or invisible one). **global.css:** added shared layout tokens (`--radius`/`--radius-sm`/`--sticky-h`), a fluid type scale (`h1`/`h2` via `clamp()`), fluid `.section` and `.grid-cards` rhythm, `.btn`/`.btn-outline` hover+active states, a global `:focus-visible` ring (2px `--c-text` — the highest-contrast token in every theme, 11.3–15.1:1 on `--c-bg`), a `prefers-reduced-motion` reduce block, and the mobile fix `@media (max-width:47.9375rem){ body{ padding-bottom: var(--sticky-h) } }` so the fixed StickyContact bar (now `min-height: var(--sticky-h)`) never overlaps the footer. **Components:** `SectionDivider` reworked to flanking hairlines around the motif glyph (fixes the ink theme, where the old glyph-only divider rendered as an invisible gap); `ProjectCard` image now sits in an `overflow:hidden` thumb that zooms `scale(1.045)` on hover with a gold title shift; `Header` nav gains hover underlines + a dropdown shadow; `Footer` links gain hover color; `MapLink`/`VideoEmbed` gain focus/hover border states; `FactsGrid` widened and collapses to one column ≤26rem; `AwardCard`/`TestimonialCard`/`PhotoGallery`/`BeforeAfter` spacing + shared-radius token cleanups (BeforeAfter captions restyled to the `.label` eyebrow treatment). **HomeView:** full-bleed hero with a quiet `--c-bg-alt`→`--c-bg` gradient wash and larger fluid headline; a `SectionDivider` now precedes every guarded home section (featured/about/awards/testimonials/contact) rather than a single one after the hero. Verified on a clean build: `npm run build` → 15 pages; `npm run check` → 0/0/0; `npm test` → 6/6; color-literal grep (`#hex|rgb|rgba|hsl` over `src/components src/views src/layouts src/styles/global.css`) → **empty** (all colors only in `themes/*.css`); `grep -riE 'undefined|null|NaN'` on `dist/index.html` + `dist/projects/sample-pond/index.html` → **empty** and the minimal fixture still renders only `<h1>` + gallery cover + lightbox scaffold (no optional block labels — the only `before`/`after`/`details` string hits are the scoped `.before-after` CSS class and the header `<details>` element, not content); the mobile body-padding rule, `:focus-visible` ring, `--sticky-h` token and reduced-motion block all confirmed present in the emitted `/_astro/index.*.css`. Flipped `site.theme` to `terracotta` then `ink`, rebuilt each → 15 pages, correct theme CSS linked, no layout breakage; restored to `stone` (`git diff site.config.ts` empty). `git diff --stat` touches only `src/{components,views,layouts,styles}` + `docs/BLUEPRINT.md` — no `content.config.ts`, `src/content/**`, `src/lib/**`, `src/pages/**`, or `site.config.ts` changes. F2 → **done**.
- **2026-07-09** — Task 13 (F10, planned → done): Built the owner-facing Sveltia CMS admin (the flagship non-technical, bilingual, phone-friendly form UI). `public/admin/index.html` — minimal page that loads `@sveltia/cms@0.170.4` (exact pinned version, checked via `npm view @sveltia/cms version` at implementation time; no floating tag) from the unpkg CDN as a `type="module"` script, with `<meta name="robots" content="noindex">` so the admin never gets indexed. `public/admin/config.yml` — the full CMS config: `backend: github` with `repo`/`base_url` left as clearly-marked `# PLACEHOLDER` values (set per instance at deploy, Task 15), `editor: preview: false` (the live-preview pane confuses on a phone), and four collections. Projects/awards/testimonials are folder collections (`format: yaml-frontmatter`, `create: true`, `_images` media folder beside the content so zod `image()` validates at build per ADR-0007); pages is a `files` collection with `about` (`body` → markdown body auto-mapped by Sveltia, plus `body_kn`/`yearsExperience`/`heroPhoto`) and `services` (an `items` `list` widget). Projects form field order = importance (title, photos, then optional fields top-to-bottom) with `facts` and `beforeAfter` as `collapsed: true` objects so the form looks short; every label is bilingual `English · ಕನ್ನಡ` and non-obvious fields carry a bilingual `hint` with an example (notably the Google-Maps "Share → Copy link" hint). **Every CMS field `name` was cross-checked against the `content.config.ts` zod schema field name — exact match on all fields of all four collections** (the #1 correctness requirement; full table in the Task 13 report; also machine-extracted the config's field names and diffed against the schema). **Services reconciliation (the cross-cutting integration point):** Sveltia's `list` widget on a `files` entry serializes as `items:\n  - …` (an `items:`-keyed mapping), but Task 3's `file()` loader for `services` expected a top-level YAML array — mismatch would make the CMS write files that fail the schema. Reconciled so both agree: rewrapped `src/content/pages/services.yml` under an `items:` key (each item's `id`/`name_en`/`name_kn` unchanged) and changed the `services` loader to `file('./src/content/pages/services.yml', { parser: (text) => (parseYaml(text) as { items: Record<string, unknown>[] }).items })` with `import { parse as parseYaml } from 'yaml'`; added `yaml@2.9.0` as a pinned dependency (exact, no caret). No service field names or item shapes changed. Verified the reconciliation didn't break the public site: clean `npm run build` → 15 pages; the About page still lists all four services — `grep 'Temple construction' dist/about/index.html` hits, and `dist/kn/about/index.html` shows the Kannada service names (`ದೇವಸ್ಥಾನ ನಿರ್ಮಾಣ` etc.) via `pickText`'s KN branch (English "Temple construction" correctly does NOT appear on the KN route — that's the i18n behavior, not a regression); the about `body` story text (`traditional guru-shishya way`) still renders on both `/about/` and `/kn/about/` (KN via the English-body fallback, no `body_kn` in the fixture). `config.yml` parses as valid YAML (`node -e "parse(readFileSync('public/admin/config.yml'))"` → OK). `dist/admin/index.html` and `dist/admin/config.yml` are both emitted on build, and the built `index.html` carries the `noindex` meta and the pinned `@sveltia/cms@0.170.4` src. `npm run check` → 0/0/0 (fixed one type error along the way: the parser must return `Record<string, unknown>[]`, not `unknown[]`); `npm test` → 11/11 (6 i18n + 5 smoke, unchanged). The **only** `src/` changes were the two-file services reconciliation (`content.config.ts` loader + `services.yml`); no other public-site code or copy touched. **HEADLESS BOUNDARY:** Step 3's interactive local-mode CMS test (open `/admin/` in real Chrome → "Work with local repository" → File System Access API → create/save/delete a throwaway entry, confirm round-trip) cannot be run headless and was NOT performed — it is documented in the Task 13 report as a manual pre-deploy checklist for Ankit. F10 → **done** (code + config complete and verified; interactive smoke test deferred to Ankit).
- **2026-07-09** — Task 12 (F13, in progress → done): Wrote `tests/smoke.test.ts`, the automated end-to-end enforcement of ADR-0007 that every prior task's manual per-page grep checks were building toward. 5 tests, gated by `describe.skipIf(!fixturesPresent)` (existence of `src/content/projects/sample-pond.md`) so a real instance that removed the sample fixtures skips this suite instead of failing: minimal fixture (title+`<img>`, none of 6 optional-block label strings, no `undefined`/`null`/`NaN`), maximal fixture (all 7 optional-block strings present), `kn` route + Kannada chrome, 404 + 6 core pages exist, home page contains the configured identity (`Shri Builders`) and nothing else. Before writing the test, built the site fresh and grepped `dist/` for every string the task brief proposed — all matched the actual emitted HTML byte-for-byte (`Method &amp; materials`, `Watch video`, etc., sourced from `src/lib/i18n.ts`'s English table), so no label-string adjustments were needed; the brief's draft test was correct as given. `package.json`'s `test:smoke` script (`astro build && vitest run tests/smoke.test.ts`) already existed from Task 1, unchanged. Proved the suite actually catches a regression (required, not skippable): temporarily broke `src/views/ProjectDetailView.astro`'s materials guard by hoisting the `<h2>{t(lang,'detail_materials')}</h2>` heading outside its `{d.materials && ...}` conditional (kept `<MaterialsChips>` itself guarded, to avoid an unrelated `undefined.map` build crash that would obscure the point) — rebuilt, and the minimal-fixture test failed with exactly the expected diff (`expected ... not to contain 'Method &amp; materials'`), all other 4 tests still green; reverted the component (`git diff` confirmed empty) and rebuilt — all 5 green again. One real fix required to keep `npm run check` clean: `tests/smoke.test.ts`'s `import { readFileSync, existsSync } from 'node:fs'` had no ambient Node types in this project (nothing previously imported a Node builtin) — `astro check` failed with `TS2307: Cannot find module 'node:fs'`; added `@types/node@^20` as a devDependency (matches `.nvmrc`'s Node 20) — `package.json`/`package-lock.json` only, no source changes. Final verification on the committed tree: `npm run test:smoke` → build (15 pages) + 5/5 smoke tests pass; `npm test` → 11/11 (6 i18n + 5 smoke — both suites now live under `tests/**/*.test.ts` per `vitest.config.ts`, so a bare `vitest run` picks up both whenever `dist/` already exists from a prior build); `npm run check` → 0 errors/warnings/hints. F13 → **done**.
- **2026-07-09** — Task 14 (F11, planned → done): Verified the existing `<Image>`/`astro:assets` pipeline (built in Tasks 6–9, never previously exercised against a real raster photo — every sample fixture is an SVG, which Astro passes through untouched) actually resizes and compresses real photos. Headless, so instead of the CMS's interactive local-mode file picker, added a throwaway project (`src/content/projects/zzz-image-test.md`, one photo) pointing at a generated `_images/imgtest.jpg` — a real 4032×3024, 4.45 MB JPEG (sharp-generated gradient+grain, same resolution/weight class as a real phone photo, verified as a genuine baseline JPEG via `file`). Built: Astro emitted 4 content-hashed `.webp` variants for it in `dist/_astro/` — `imgtest.*_s1xYw.webp` (800×600, 3.25 KB), `imgtest.*_Jkqct.webp` (1200×900, 26.4 KB), `imgtest.*_ZzWpIV.webp` (400×300, 0.86 KB), and a full-resolution `imgtest.*_Z2mioAm.webp` (4032×3024, 3.27 MB — 26% smaller than the original from format re-encoding alone, used as the `<img src>` fallback for browsers without srcset support). Confirmed in the built HTML: the project-detail gallery `<img>` carries `srcset="…_s1xYw.webp 800w, …_Jkqct.webp 1200w"` + `width="4032" height="3024"`; the projects-listing card `<img>` carries `srcset="…_ZzWpIV.webp 400w, …_s1xYw.webp 800w"` + the same explicit `width`/`height` — both real multi-width `srcset`s and explicit intrinsic dimensions (prevents layout shift), on both `PhotoGallery` and `ProjectCard`. The actual bytes a mobile browser fetches from these `srcset`s (3.25–26.4 KB) are 99.4–99.9% smaller than the 4.45 MB original — the original file is never what ships to a visitor. One incidental, harmless finding: the build also emits one unreferenced original-format copy of the source (`imgtest.<hash>.jpg`, same 4.45 MB, not linked from any built page or CSS) — dead weight in the deploy output, not served to real visitors, out of scope for this task (no code changed). **Owner-upload rule (added for F15/onboarding):** owners upload phone photos as-is, unresized and uncompressed — the Astro build pipeline handles all resizing/compression automatically at deploy time, no owner action needed. Cleanup: `zzz-image-test.md` and `_images/imgtest.jpg` removed after verification; `git status` clean; rebuilt clean → 15 pages (back from 17 with the throwaway); `npm run check` → 0/0/0; `npm test` → 11/11. No public-site code or image components were changed — this task only verified and documented existing behavior. Full evidence (exact file names/sizes, built-HTML excerpts) in the Task 14 report.
- **2026-07-09** — Task 15 (F14, F15, planned → done): Final task — deployment and owner-onboarding runbooks, documentation only, no code/schema/content changes. Read the real repo state first (per the brief's accuracy constraint) before writing anything: `package.json` scripts (`dev`/`build`/`preview`/`check`/`test`/`test:smoke`, deps `astro@^5.18.2`/`yaml@2.9.0`, devDeps `@astrojs/check`/`@types/node`/`typescript`/`vitest`), `.nvmrc` (`20`), `site.config.ts`'s exact `SiteConfig` fields (`name`/`tagline`/`owners`/`phone`/`whatsapp`/`email?`/`region`/`url`/`logo?`/`theme`/`languages`), `public/admin/config.yml`'s two placeholder lines (`backend.repo: OWNER/REPO` and `backend.base_url: https://sveltia-cms-auth.WORKER.workers.dev`, both marked `# PLACEHOLDER`), `public/admin/index.html`'s pinned `@sveltia/cms@0.170.4` CDN version, ADR-0003/0004 (Sveltia GitHub auth model, Cloudflare Pages rationale), and the Task 13 report's deferred manual interactive-CMS-test checklist (folded into `docs/DEPLOYMENT.md` §7 rather than duplicated ad hoc). Confirmed all four `(Sample)`-suffixed content files (`sample-temple.md`, `sample-pond.md`, `sample-award.md`, `sample-testimonial.md`) by grep before writing the per-instance delete checklist. **`docs/DEPLOYMENT.md`** (new): 7-section numbered runbook — (1) create GitHub repo + the one sanctioned push; (2) Cloudflare Pages project creation with the exact build command/output dir/Node version; (3) GitHub OAuth app + callback URL; (4) `sveltia-cms-auth` worker deploy via `wrangler deploy`/`wrangler secret put`/`ALLOWED_DOMAINS`; (5) filling the two real `config.yml` placeholders + commit/push; (6) the per-instance checklist (every `site.config.ts` field, sample-content deletion by exact filename, custom domain later); (7) the pre-launch interactive CMS smoke test (sign-in, throwaway project, ~1-minute rebuild wait, delete, Services/About round-trip check) — explicitly the same manual test Task 13 could not run headless, now given as Ankit's final gate before handing a phone to an owner. **`docs/ONBOARDING.md`** (new): owner-facing, English + Kannada line-by-line throughout (reusing the exact bilingual vocabulary already established in `src/lib/i18n.ts`'s `kn` table and `public/admin/config.yml`'s collection labels, e.g. `Projects · ಯೋಜನೆಗಳು`, for consistency with what the owner will actually see on-screen) — Part 1 one-time setup (GitHub access, open `/admin` in Chrome, sign in once, Add to Home Screen) done by/with Ankit; Part 2 an 8-step solo walkthrough (open app → Projects → + New → name → photo → Save → wait ~1 min → see it live) matching the real form's minimum requirement (title + 1 photo, rest optional); closes with an explicit "nothing is ever lost, call Ankit" reassurance in both languages (grounded in the real git-commit-per-save architecture, not a platitude) and a quick-reference table. **`README.md`** (new, repo previously had none): ~14 lines — what the project is, the four npm scripts, links to `docs/BLUEPRINT.md`, `docs/adr/`, `docs/DEPLOYMENT.md`, `docs/ONBOARDING.md`. **Final full-suite gate** (fresh caches: `rm -rf dist .astro node_modules/.astro` first): `npm run check` → 0 errors/0 warnings/0 hints; `npm run build` → 15 pages; `npm test` → 11/11 (6 i18n + 5 smoke, `dist/` present from the just-completed build); `npm run test:smoke` → rebuilds (15 pages) + 5/5 smoke tests. All green, no regressions. **Ledger final pass:** re-read every F1–F15 row against this task's fresh build/check/test output and the cited files/commands above — all thirteen `done` rows (F1–F13) still match current repo state (page count, test count, pinned versions unchanged since their respective tasks); F14/F15 updated from `planned` to `done` in this task (see their rows above) since the *documentation* deliverable is complete, though the *actual* Cloudflare deploy and phone onboarding remain real-world actions Ankit performs later using these runbooks — no gap between what the ledger claims and what exists in the repo today. No remote was added and nothing was pushed (repo stays local-only, per the task's explicit constraint); `git remote -v` confirmed empty both before and after this task's file changes.
- **2026-07-09** — Final whole-branch review cleanups (4 fixes, no new features). **(1) `npm test` reproducibility (important):** the previous `vitest.config.ts` glob (`tests/**/*.test.ts`) pulled `tests/smoke.test.ts` into the default `npm test`, which reads `dist/` and threw ENOENT on a clean checkout with no prior build — only `npm run test:smoke` (which builds first) was ever a valid way to run it. Narrowed `vitest.config.ts`'s `include` to `['tests/i18n.test.ts']` only, and added a sibling `vitest.smoke.config.ts` (`include: ['tests/smoke.test.ts']`) that `test:smoke` now points at explicitly (`astro build && vitest run --config vitest.smoke.config.ts`) — a plain positional-path filter wouldn't have worked since vitest's CLI filters only narrow within the config's own `include`, not override it. Verified both independently: `rm -rf dist && npm test` → 6/6 i18n tests pass with no `dist/` on disk at all (proves the fix); `npm run test:smoke` → build (15 pages) + 5/5 smoke tests pass. **(2) i18n'd aria-labels (ADR-0008):** `Header.astro`'s hamburger `aria-label="Menu"` and `PhotoGallery.astro`'s lightbox-close `aria-label="Close"` were hardcoded English, announced in English even on `/kn/*` routes. Added `a11y_menu`/`a11y_close` to both `en`/`kn` tables in `src/lib/i18n.ts` (`Menu`/`ಮೆನು`, `Close`/`ಮುಚ್ಚಿ`); `Header.astro` now calls `t(lang, 'a11y_menu')`. `PhotoGallery.astro` had no `lang` prop (only `photos`/`title`) — added one and threaded it from its single caller, `ProjectDetailView.astro` (`<PhotoGallery photos={d.photos} title={d.title} {lang} />`), then swapped the close button to `t(lang, 'a11y_close')`. Verified in built output: `dist/index.html` → `aria-label="Menu"`, `dist/kn/index.html` → `aria-label="ಮೆನು"`; `dist/projects/sample-temple/index.html` → `aria-label="Close"`, `dist/kn/projects/sample-temple/index.html` → `aria-label="ಮುಚ್ಚಿ"`. **(3) `VideoEmbed.astro` typo:** the lazy-iframe's `allow` list had `accelerate;` — not a real Permissions-Policy feature — instead of `accelerometer;`; fixed, rest of the allow list (`autoplay; encrypted-media; picture-in-picture`) untouched. **(4) Dead code removed:** `nav_home` dropped from both `en`/`kn` tables in `src/lib/i18n.ts` (confirmed unused via `grep -rn "nav_home" src/` — only the two definitions existed; the header's brand link doesn't use it). `public/admin/config.yml`'s global `media_folder: src/assets/uploads` / `public_folder: /src/assets/uploads` (lines 7–8) removed — dead, since every one of the four collections (projects/awards/pages-about/pages-services) already sets its own `media_folder`/`public_folder`; confirmed via `grep -n "media_folder\|public_folder"` that only the four per-collection pairs remain, and the file still parses (`yaml`'s `parse()` on the file → `OK`). **Full gate on the final tree:** `npm run build` → 15 pages; `npm run check` → 0/0/0; `rm -rf dist && npm test` → 6/6 with no `dist/`; `npm run test:smoke` → 15-page build + 5/5; `grep -rInE '#[0-9a-fA-F]{3,8}|rgba?\(|hsla?\(' src/components src/views src/layouts src/styles/global.css` → empty (no color regressions introduced). Explicitly out of scope for this pass (left as logged minors, not touched): the language-switcher two-language-only label, `--c-scrim` reuse, the projects filter's "Other" bucket. Repo stays local-only — no remote configured, nothing pushed.
- **2026-07-10** — F16 (planned → in progress): Brainstormed and approved trilingual content. ADR-0009 (`i18n` sidecar, per-field English fallback, empty-means-absent) and the design spec written and committed before any code, per the documentation workflow. Auto-translate scoped out for now — free backends (Bhashini, MyMemory) exist and the fallback makes the button safe to add later.
- **2026-07-10** — F16 (in progress → done): Implemented trilingual content. Added `lib/localize.ts` (+ `lib/localize.test.ts`, 13 tests, run via a new `npm test` → `node --test`), `lib/translatable.ts`, `components/admin/TranslationPanels.tsx`; `i18n` sidecar types; localize on the read path in all seven public pages; `readTranslations()` on the write path in all five forms. One defect found and fixed during verification: localized records carried their `i18n` sidecar into client-component props, shipping every language's text to every visitor — `overlay()` now strips it (test added). One near-miss worth recording: a verification POST that omitted unchanged English fields silently blanked them (`updateProject` rebuilds the record from the form), caught by diffing against a backup and re-submitted in full; the admin form itself always submits every field, so this only affects hand-crafted POSTs.
- **2026-07-10** — Bugfix (F16/F9): language switching was intermittent and needed a manual refresh. Root cause found with a real browser (not curl): `/kn/…` and `/hi/…` are middleware **rewrites** onto the same underlying route, so Next answers their RSC requests with `x-nextjs-rewritten-path: /…`. Next's client router therefore filed all three languages under one cache key — the RSC payload came back correctly translated (verified: `curl -H 'RSC: 1' /kn/?_rsc=` contains ನಮ್ಮ ಕೆಲಸಗಳು) but the router reused whichever language tree it already had, changing the URL and nothing else. Reproduced deterministically both directions: fresh `/` then click ಕನ → URL `/kn/`, page English; fresh `/hi/` then click EN → URL `/`, page Hindi. A hard refresh always worked, because a document request bypasses the router cache. Fix: the public site no longer routes through the client router — `next/link` replaced with plain `<a>` in the site layout (brand + nav), home page, ProjectGrid cards, LanguageLinks and not-found. Every public navigation is now a document request and the server reads the language off the path. This also removes `?_rsc=` prefetches, which on a static host return HTML rather than a payload (the other half of the 'sometimes' behaviour). The admin keeps `next/link` — it has no rewrites and is never part of the static build. Constraint documented in `lib/paths.ts` and `components/LanguageLinks.tsx`. **Verified** with headless-Chromium click-throughs on both `npm start` and the generated `out/` (via `npm run preview`): switching language keeps the current page, switching pages keeps the language, in all three languages and from any starting language; 0 `_rsc` requests remain; 13/13 unit tests still pass.
- **2026-07-10** — Stability hardening (post-review, 3 parallel review agents). (1) `lib/db.ts`: `readContent` now normalizes shape — missing top-level keys (`awards`, `services`, `projects`, `testimonials`, absent `about.body`) default to `[]`/`""` instead of crashing. Confirmed against the running server: each of those hand-edited/partial files previously 500'd the whole site (home, about, projects, awards depending on the key); all now render 200. Genuinely corrupt JSON still throws (loud) rather than returning a silent-empty structure the admin could save over real content. (2) `lib/db.ts`: `writeContent` is now atomic (temp file + `rename`), so an interrupted save can never leave a truncated, unparseable content.json. (3) `lib/actions.ts` `updateAbout`: `body` now falls back to the existing text on an empty submit (`reqStr || existing`, matching every other required field) — an accidental empty Save no longer wipes the About story. Verified via a real form POST: empty body preserved, real edit still saves. (4) `app/globals.css` `.not-found`: `padding: 5rem 0` → `padding-block: 5rem` — the shorthand was zeroing the side gutter inherited from `.wrap` (same class of bug as the earlier `.section` fix), leaving the deployed 404 page flush to both screen edges at 360px. Verified: heading now 20px from edge, no overflow. Reviewers also confirmed the static pipeline is sound (snapshot.mjs kills its server in `finally`, fails loud on non-200, wipes `out/` each run so no stale pages; preview.mjs blocks traversal). Deferred, not fixed: the six earlier UX/i18n polish items (html lang, page titles, hardcoded name, back-link, JS-off filters, image resizing), a local-admin auth-bypass via `/kn/admin` (middleware lang-rewrite short-circuits the auth check — only reachable on the local `npm start`, since the deployed site is static with no admin/middleware), and JS-off photo-add in the admin.
- **2026-07-10** — Security fix (`middleware.ts`): closed a local admin-auth bypass. The public language rewrite matched `^/(kn|hi)(/.*)?$`, which also caught `/kn/admin/*` and `/hi/admin/*`; it rewrote to the real `/admin/*` route and `return`ed *before* the auth block, and middleware doesn't re-run on an internal rewrite — so `/kn/admin/projects` served the full dashboard (and its server-action POSTs) with no session cookie. Fix: the lang rewrite now skips paths whose rest starts with `/admin` (the admin is not localized), so those URLs fall through to a 404 — no admin content without the cookie. Only ever reachable on the local `npm start`; the deployed site is static with no middleware. Verified against the running server: `/kn/admin/`, `/hi/admin/settings/`, `/kn/admin/projects/seed-temple/` → 404, no admin markup; `/admin/projects/` with no cookie → redirect to `/admin/login/`; with the cookie → 200 dashboard; public `/kn/`, `/hi/`, `/kn/projects/`, `/hi/about/` → 200 and localized. 13/13 tests pass.
- **2026-07-11** — F17 (planned → in progress): Brainstormed and approved the Gallery. Standalone photos only (not an aggregate of project photos); no captions; rotate is a temporary client-side view control in the lightbox, not persisted. Spec written and committed before code, per the documentation workflow.
- **2026-07-11** — F17 (in progress → done): Implemented the Gallery. New `Content.gallery`, admin `/admin/gallery` (reusing ProjectPhotos), public `/gallery` (reusing Lightbox), `updateGallery` action, snapshot path, 5 i18n keys. Lightbox: temporary rotate control + background scroll-lock + focus trap/restore + near-opaque scrim. Note: discovered mid-task that the owner had been editing content via their own server on :3000 (real contact info, tagline translated to kn/hi, test projects cleared) — that live data was preserved and deliberately NOT committed with the feature code.
- **2026-07-11** — Two small edits + an image-lifecycle fix. (1) **Editable services (What We Do):** the `services` list had no admin UI. Added `components/admin/ServicesEditor.tsx` (fixed rows: existing + 2 spare, English name/blurb + collapsed kn/hi panels, no JS) into the Site info page; `lib/services.ts` `parseServices()` rebuilds the list from indexed `service.<i>.<field>` rows (empty name = dropped, reuses the id at each position) and is wired into `updateSiteInfo`. Pure-function so it's unit-tested (7 tests) rather than round-tripped against live data. (2) **Years of experience → free text:** `About.yearsExperience` is now `string` ('35+', 'thirty five'); db normalizer coerces old numeric values; the public render dropped the hardcoded '+'. (3) **Server-action body limit:** raised to 50mb (`experimental.serverActions.bodySizeLimit`) — phone photos are several MB and were 413ing at the 1MB default. (4) **Lazy images:** `loading="lazy"` on grid/card/gallery/before-after/about-hero (home hero stays eager); verified present in generated `out/`. (5) **Orphaned uploads:** removing a photo / deleting a project/award / replacing the About hero / removing a gallery photo now deletes the file from disk (`lib/uploads.ts`, diff-based, never deletes a still-referenced or pre-orphaned file; 8 unit tests). **Data-loss incident (logged for honesty):** while testing the delete behaviour on the shared port-3457 server, the owner was concurrently uploading gallery photos via their own port-3000 server (same data/content.json + uploads dir). The delete test removed 2 of the owner's live gallery photo files, which were unrecoverable; content.json was made consistent (dead refs dropped) and the owner asked to re-upload. Root cause: testing destructive admin actions against live data during concurrent editing. Protocol going forward: no admin-write testing against the live checkout — use an isolated worktree/copy or read-only GETs. |
- **2026-07-11** — Image compression on upload (`lib/actions.ts` `saveUpload`). Raster photos are now downscaled (long edge capped at 2000px, `withoutEnlargement` so small images aren't upscaled) and re-encoded (JPEG q82 mozjpeg / PNG level 9 / WebP q82) via sharp before writing; `.rotate()` bakes in EXIF orientation, so sideways phone photos also come out upright. SVG and GIF pass through untouched; any sharp failure falls back to the original bytes so an upload is never lost. sharp 0.34.5 was already installed (Next pulls it in) and verified working here (libvips 8.17.3); added `serverExternalPackages: ['sharp']` so Next keeps the native module external. Verified the exact pipeline standalone: a 4032×3024 ~2MB JPEG → 2000×1500 309KB (85% smaller), long edge capped, 400×300 not upscaled, PNG stays PNG; the `(await import('sharp')).default` runtime form resolves the sharp function; build clean, 28 tests pass. Resolves the earlier deferred 'images served full-size' perf item and complements the 50mb body-limit fix (uploads now arrive large but are stored small). |
