ADMIN_PASSWORD=temple123
SESSION_SECRET=change-this-to-a-long-random-string-in-production

# The real public address of the site, no trailing slash. Used for the WhatsApp/
# Facebook share preview (og:image must be an absolute URL) and sitemap.xml.
# Must be set before `npm run generate`, or those are skipped with a warning.
SITE_URL=https://example.pages.dev
